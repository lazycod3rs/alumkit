# Usage

Document the primary :package_name workflow here.

```php
// Add a focused example for :package_slug once the package API is defined.
```

Keep examples short and copyable. Prefer showing how the package is used inside a normal Laravel application instead of documenting internals.

## Configuration

If :package_name publishes configuration, explain the important options and their defaults here.
